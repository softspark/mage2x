version: 2
updates:
  # Keep GitHub Actions current (incl. the SHA-pinned third-party release action).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: chore
